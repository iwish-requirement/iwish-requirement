# 数据统计口径说明

本文档是当前需求系统统计口径的统一说明，适用于公司总览、部门总览、部门成员统计和动态字段统计。业务展示与数据核对应以线上数据库当前记录、部门状态配置和本文口径共同为准。

## 1. 通用时间边界

- 所有自然日和自然月均按北京时间（UTC+8）计算。
- 月度区间使用左闭右开边界，即当月 1 日 00:00:00（含）到下月 1 日 00:00:00（不含）。
- 状态是否属于“已完成”“处理中”或“已延期”，以需求所属部门的状态配置为准，不使用全系统写死的单一状态名称。
- 统计接口以稳定的需求内部 ID 分页读取全量数据，不依赖 Supabase 默认 1000 条返回上限；动态字段统计也不再使用 5000 条截断。

## 2. 公司/部门总览

| 指标 | 当前统计方式 |
| --- | --- |
| 新增需求数 | `created_at` 落在所选月份内的需求数。 |
| 已完成需求数 | 当前状态属于部门“已完成”状态，且 `finished_at` 落在所选月份内的需求数。 |
| 处理中需求数 | 当前状态属于部门“处理中”状态，且 `created_at` 落在所选月份内的需求数。 |
| 已延期需求数 | 当前状态属于部门“延期”状态，且 `created_at` 落在所选月份内的需求数。 |
| 完成率 | 所选月份完成数 ÷ 所选月份新增数。两者是不同事件月份，因此跨月集中交付时可能超过 100%。 |
| 延期率 | 所选月份新增且当前处于延期状态的需求数 ÷ 所选月份新增数。 |
| 平均交付周期 | 所选月份完成的有效需求中，`created_at` 到 `finished_at` 的平均天数。 |
| 平均分配耗时 | 所选月份完成的有效需求中，`created_at` 到 `assigned_at` 的平均小时数。 |
| 平均响应耗时 | 所选月份完成的有效需求中，`created_at` 到 `started_at` 的平均小时数。 |
| 平均处理耗时 | 所选月份完成的有效需求中，`started_at` 到 `finished_at` 的平均小时数。 |
| 评分均值 | 所选评分周期内，每条有效评分先取其维度均值，再计算总体均值。 |
| 评分覆盖率 | 所选评分周期内已完成评分任务数 ÷ 全部评分任务数。 |

最近六个月趋势中，“需求数”按 `created_at` 归月，“完成数”按 `finished_at` 归月。客户、部门、提交人、项目和需求类型排行均基于所选月份内新建的需求。

## 3. 部门成员统计

成员统计先按部门配置确定月份归属方式，再按需求当前执行人聚合：

- `created`：按提交月份归属。
- `scheduled`：按内部排期开始日期归属；创意/设计部门当前使用此方式。
- `finished`：按完成月份归属。

每条需求在选定月份内计入当前执行人一次。“已完成需求”仍以需求所属部门配置的已完成状态为准；平均交付周期使用 `created_at` 到 `finished_at`。创意部会同时展示已完成素材需求数、已完成素材数量、已完成视频需求数和已完成视频数量。

### 3.1 素材与视频分类

- 需求属于素材、视频、混合或不计入交付量，优先读取需求类型配置中的 `deliveryCategory`。
- 当前创意部需求类型已在数据库中完成分类配置；名称或编码推断仅作为旧数据兼容兜底。
- 素材数量优先读取素材/图片数量字段，视频数量优先读取视频数量字段。
- 两类数量都缺失时，系统会兼容参考素材集合或附件数量，避免历史需求完全丢失工作量。
- 只有状态属于“已完成”的需求才会进入创意部四项完成指标。

## 4. 内部排期开始日期的作用

内部排期开始日期是创意部安排制作和核算成员月度工作量的日期，不是需求提交人的交付截止日期，也不是系统自动推算的完成日期。

它只影响以下内容：

- 创意部成员统计中，这条需求及其素材/视频工作量归属于哪个月份。
- 管理者查看排队、排期和成员月度承载情况时的时间位置。

它不会直接改变以下内容：

- 部门总览的新增需求月份，新增仍按 `created_at`。
- 部门总览的完成需求月份，完成仍按 `finished_at`。
- 需求状态、截止日期、完成日期、评分或实际交付记录。

例如：需求 7 月 30 日提交，内部排期为 8 月 5 日，8 月 9 日完成，则 7 月总览新增数加 1，8 月总览完成数加 1，创意部成员 8 月工作量加 1。

内部排期由执行人、管理员或部门负责人在需求详情/接单面板维护。提交人创建需求时不能填写该字段，复制需求时也不会继承旧排期。历史需求缺少或无法解析排期时，成员统计会回退到提交月份，并在统计页面标明回退数量，以保持历史数据兼容；补录排期后，该需求会移动到新的成员工作量月份。

## 5. 动态字段统计

动态字段统计以当前部门启用模板中的可导出字段为统计维度，支持状态、创建日期、截止日期和动态字段值筛选。多选值会逐项计数，每个字段最多展示出现次数最高的 50 个值；`totalDemands` 是满足筛选条件的全量需求数。

## 6. 数据准确性约束

- 新建、导入、复制和更新需求时，需求类型必须存在且属于当前需求部门。
- 数据库外键和校验触发器作为最终防线，阻止空需求类型和跨部门需求类型写入。
- 历史缺失需求类型已通过可审计迁移补齐，补齐记录保留在审计表中。
- 统计读取使用全量分页；分页查询固定按内部主键排序，避免重复或遗漏。
- 若业务状态配置、需求类型交付分类或排期字段发生变更，应同时更新配置、迁移记录、产品更新日志和本文档。
